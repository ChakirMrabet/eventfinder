import * as actionTypes from "../common";

const defaultState = {
  selectedEvent: null
};

export default (state = defaultState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
